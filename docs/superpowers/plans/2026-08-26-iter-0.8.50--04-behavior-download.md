# 子计划 04：用户体验增强计划（行为采集）与下载引擎增强

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development / executing-plans. `- [ ]` 跟踪。

**Goal:** 设置页新增"用户体验增强计划"开关（含声明书，默认关闭；同意即开启采集）；客户端对行为事件**gzip 压缩后攒批，每隔一段时间批量上传**至后端；同时增强下载引擎（更多镜像源 + 断点稳健性）。

---

## 任务 1：体验增强计划声明书 + 开关

**Files:**
- Modify: `/workspace/photo-tool/app/src/main/res/values/strings.xml`
- Modify: `/workspace/photo-tool/app/src/main/res/values/arrays.xml`
- Modify: `/workspace/photo-tool/app/src/main/java/com/photo/tool/Prefs.java`

- [ ] **Step 1: 声明书文案（strings.xml）**

新增 `string/ux_plan_declaration`，写明：采集内容(分辨率/画质/滤镜/主题/版本等非隐私技术数据)、用途(改进体验)、绝不采集照片像素内容与通讯录、存储(匿名 uid 关联、后端 D1 压缩分批)、可随时在设置关闭、关闭后清空本地待传缓存。

- [ ] **Step 2: `Prefs` 新键**

```java
public static boolean uxEnabled(Context c) { return sp(c).getBoolean("ux_enabled", false); }
public static void putUxEnabled(Context c, boolean v) { sp(c).edit().putBoolean("ux_enabled", v).apply(); }
```

- [ ] **Step 3: 设置页新增一行**：首开时弹 `AlertDialog` 出示声明书，[同意]才置 `ux_enabled=true`；再次点开关为直接关闭。关闭时调用 `BehaviorCollector.clearPending()`。

- [ ] **Step 4: Commit**

```bash
cd /workspace/photo-tool && git add app/src/main/ && git commit -m "feat(ux): 用户体验增强计划声明书与开关"
```

---

## 任务 2：行为采集器（压缩 + 攒批 + 定时上传）

**Files:**
- Create: `/workspace/photo-tool/app/src/main/java/com/photo/tool/BehaviorCollector.java`

- [ ] **Step 1: 事件模型与本地攒批**

```java
public final class BehaviorCollector {
    private static final int BATCH_BYTES = 32 * 1024;    // 32KB 触发上传
    private static final long MAX_AGE_MS = 10 * 60 * 1000; // 或距上次≥10min
    private static String pending(String uid) { /* 内存+磁盘双缓冲 File cacheDir/ux-batch.jsonl */ }

    public static void log(Context c, String type, JSONObject payload) {
        if (!Prefs.uxEnabled(c)) return;
        // 组装 {ts,type,payload}，追加到本地 jsonl 缓冲
    }
    public static void flushIfDue(Context c) { /* 达阈值或超时→compressAndUpload(c) */ }
    private static void compressAndUpload(Context c) {
        // 读缓冲→GZIPOutputStream→ByteArrayOutputStream→base64
        // 附 Authorization: Bearer Prefs.token(c)
        // POST /behavior/ingest 成功→清缓冲；失败(429/5xx)→保留本次缓冲待下次重试
    }
    public static void clearPending(Context c) { /* 关闭计划时清空磁盘缓冲 */ }
}
```

- [ ] **Step 2: 触发器**：在 `CameraActivity`（拍照/超分/滤镜/主题切换）、`ResultActivity`(保存)、`SettingsActivity` 变更点调用 `BehaviorCollector.log(...)`；在 `CameraActivity` 的线程池空闲回调与 `onResume` 调 `flushIfDue`。

- [ ] **Step 3: Commit**

```bash
cd /workspace/photo-tool && git add app/src/main/java/com/photo/tool/BehaviorCollector.java && git commit -m "feat(ux): 行为采集器gzip压缩/攒批/定时上传"
```

---

## 任务 3：下载引擎——更多镜像源

**Files:**
- Modify: `/workspace/photo-tool/app/src/main/java/com/photo/tool/UpdateChecker.java`
- Modify: `/workspace/photo-tool/app/src/main/java/com/photo/tool/DownloadService.java`
- Modify: `/workspace/photo-tool/app/src/main/java/com/photo/tool/Prefs.java`

- [ ] **Step 1: 镜像源表扩充（现 4 源 → 8 源）**

在 `UpdateChecker` 现镜像基础上追加：`ghfast.top`、`ghproxy.net`、`gh.llkk.cc`、`gitproxy.click`。构建统一 `mirrors()` 列表，测速/自动选择逻辑复用现有"并发测速取最快"。

- [ ] **Step 2: `Prefs.downloadMirror` 上限 3→7；设置页下拉同步扩充（`arrays.xml`**）。
- [ ] **Step 3: 断点稳健性**：`DownloadService` 现有的 `dl_offset` 断点在重试前校验上游 `Accept-Ranges` 与 `Content-Range`；分片下载任一分片连续失败≥3 次时整体回退该镜像到下一候选源并保留已完整分片；下载完成做 `sha256` 校验（沿用 `version.json` 的 sha），不匹配则清 `dl_offset` 全量重下一次并提示错误码。

- [ ] **Step 4: Commit**

```bash
cd /workspace/photo-tool && git add app/src/main/ && git commit -m "feat(download): 扩充至8镜像源并加固断点/分片/完整性重试"
```

---

## 验收
- [ ] 开启体验增强计划前，不产生任何网络上报；同意声明书后方开启。
- [ ] 产生事件→本地缓冲→达 32KB 或 10min 自动压缩上传→成功清空；失败保留缓冲可重传。
- [ ] 关闭计划即停止采集并清空本地待传缓存。
- [ ] 更新下载可在 8 镜像源间测速择优；断点续传遇上游不支持分片时正确回退全量；sha 校验失败自动重下一次并给出错误码诊断。