package com.photo.tool;

/** 插件类别：滤镜特效 / 新增功能 / 主题换肤。 */
public enum PluginKind {
    /** 像素级滤镜/特效插件（如黑白、复古） */
    FILTER,
    /** 应用功能插件（如日期水印等新能力） */
    FEATURE,
    /** UI 主题插件（全局换肤） */
    THEME
}